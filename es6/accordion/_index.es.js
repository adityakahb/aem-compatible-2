import { __ArrayCall, __AddClass, __HasClass, __RemoveClass } from './../_constants.es';

export default class Accordion {
  constructor() {}
  __init() {
    const elements = __ArrayCall(document.querySelectorAll('[data-accordion="true"]') || []);
    elements.forEach(element => {
      
    });
  }
}